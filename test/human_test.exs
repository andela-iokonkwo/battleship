defmodule BattleshipHumanTest do
  use ExUnit.Case
  doctest Battleship.Human

  test "layout_ships" do
    # ships_info = Battleship.Human.generate_fleet(8, 4)
    # IO.inspect ships_info
    # assert  Enum.count(ships_info) == 8
  end
end
